// Arquivo de configuração para a rota dinâmica de GRC assessments
// Impede que o Next.js tente gerar esta rota estaticamente durante o build
export const dynamic = 'force-dynamic';
export const dynamicParams = true;

// Esta configuração é equivalente a dizer ao Next.js que esta é uma rota completamente dinâmica
// e que não deve tentar gerar caminhos estáticos para ela durante a compilação